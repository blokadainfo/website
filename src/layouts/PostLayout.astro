---
import Layout from '@layouts/Layout.astro';
import Navbar from '@components/custom/navbar/navbar.svelte';
const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title}>
  <Navbar homepage={false} client:load />
  <article class="mx-auto my-8 max-w-4xl px-6 tracking-tight hyphens-auto">
    <header class="mb-8 text-center">
      <div class="grid w-full">
        <img
          src={frontmatter.image}
          alt="Naslovna slika"
          class="col-start-1 row-start-1 w-full object-cover"
        />
        <div class="col-start-1 row-start-1 h-full bg-gradient-to-t from-black to-transparent">
        </div>
        <!-- <h1
          class="col-start-1 row-start-1 self-end p-3 text-left text-2xl font-bold text-balance text-white sm:text-4xl"
        >
          {frontmatter.title}
        </h1> -->
      </div>
      <div class="text-md text-gray-500">
        <span>{frontmatter.date}</span> • <span>{frontmatter.author}</span>
      </div>
    </header>
    <section class="prose prose-lg max-w-none">
      <slot />
    </section>
  </article>
  <footer class="my-8 text-center">
    <p class="text-sm text-gray-500">Autor {frontmatter.author} | {frontmatter.date}</p>
  </footer>
</Layout>
